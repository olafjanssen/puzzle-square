// GameSettings; definition of a single game

function GameSettings(deck, gridSize, setupType, imageClassName){
	this.deck = deck;
	this.gridSize = gridSize;
	this.setupType = setupType;
	this.imageClassName = imageClassName;
}
